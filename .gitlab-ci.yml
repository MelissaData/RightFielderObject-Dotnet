stages:
  - build
  
buildJob:
  stage: build
  image: mcr.microsoft.com/dotnet/sdk:10.0
  
  script:
    - dotnet publish -f="net10.0" -c Release -o Artifacts MelissaRightFielderObjectWindowsDotnet/MelissaRightFielderObjectWindowsDotnet.csproj

  artifacts:
    paths:
      - Artifacts/
    expire_in: 1 week
    
  tags:
    - docker-linux
